pages:
  stage: deploy
  script:
    - mkdir -p public

    - cp -r *.html *.css public/
    - cp -r images/ public/

  artifacts:
    paths:
      - public

  rules:
    - if: $CI_COMMIT_BRANCH == "main"