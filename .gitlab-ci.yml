variables:
  JEKYLL_ENV: production

stages:
  - build

pages:
  stage: build
  script:
    - bundle config set --local path 'vendor/bundle'
    - bundle install

    - bundle exec jekyll build -d public
    
  artifacts:
    paths:
      - public
      
  only:
    - main
